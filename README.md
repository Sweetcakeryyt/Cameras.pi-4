# Cameras.pi-4
#THIS IS ONLY FOR RASPBERRY PI 4
#This is code only accesable with a Raspberry pi 4 to access cameras.
#THIS IS NOT A APP WITH A INSTALL WIZARD.
#You download the code file. use word (universal/recomended),or any app you have that can save large text files and put the code into the Raspberry pi's code.
#You may need to use windows CMD to download this. "pip install opencv-python"
